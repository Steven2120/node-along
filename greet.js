const getInput = require("./get-input.js");

const userName = getInput();
console.log("Hello " + userName);

// Now when you run in node mental-age.js followed by something
// to take in the place for the str then it returns "Hello + str"

